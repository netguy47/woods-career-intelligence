# PBS Scorer Revision 4.3 — Independent Review Package

This package is frozen for human review of `calib-01` through `calib-05` and `holdout-01`.

- Gate: `independent_review`.
- All builder-generated material is classified as internal evidence.
- No labels, fixtures, policy, scorer logic, retrieval logic, thresholds, weights, or protected files are included for modification.
- No external search, live search, trial, holdout replay, or parameter experiment was performed for this package.
- `holdout-01` is represented only by its existing recorded artifact; no additional holdout material was inspected.

Files:

- `review_packet.md` — frozen case records, labels, policy gates, traces, provenance status, and counterfactuals.
- `adjudication_form.md` — blank human adjudication form; no suggested answers.
- `protected_checksums.sha256` — protected-file hashes and package-file hashes.
- `package_inventory.json` — package inventory and scope declarations.
